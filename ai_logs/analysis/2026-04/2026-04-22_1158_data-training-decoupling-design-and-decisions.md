### 分析 9

时间：2026-04-22 11:58:52 +08:00
来源类型：提问
来源说明：用户在本次会话中明确“数据处理与模型训练解耦”的目标边界，并确认新入口命名、兼容与风险策略。

分析类型-目的：方案设计
分析类型-产出：决策建议
结论状态：可用

输入内容：
- 目标：运行时强隔离；数据处理覆盖完整数据集处理过程；训练模块只负责训练；支持断点继续训练。
- 约束：训练阶段不允许触发数据处理；旧命令可继续使用；新命令独立文件；先单仓内解耦；Web 旧 API 暂时保留。
- 偏好：可读型数据工件命名；后续 Web 端希望以卡片化方式选择与展示数据版本。

分析结果：
- 新入口采用复制重命名后独立演进，避免影响现有主链路：
  1. `scripts/run/run_data_artifact.py`
  2. `scripts/run/run_train_artifact.py`
  3. `scripts/run/run_resume_artifact.py`
  4. `scripts/run/run_hyper_artifact.py`
- 数据工件包含完整训练消费所需内容：已处理后的 `train/valid/test` 数据、缩放器参数（可逆）、必要元信息（`meta.json`）。
- 训练与续训默认执行严格一致性校验（不一致直接报错），同时允许“强制复用（高风险）”手动开关以提供灵活性。
- 兼容性策略：旧脚本与旧 API 保持可用；新链路独立，不与旧链路互相耦合。
- 续训语义：以已有保存模型为基础继续训练，建议以完整 `run_id` 作为续训目标定位键。
- 建议的数据工件可读命名：`da_YYYYMMDD_HHMMSS__<dataset>__<dataset_class>__<sig8>`。
- 建议 Web 卡片展示字段：时间戳（主标题）、`dataset/dataset_class`、`seed`、`split`、`window`、`scaler`、状态、批次数、备注。

关键结论：
1. 仅依赖 `dataset_class` 判断复用不充分，建议采用“数据签名（data_signature）+ 严格校验”为默认路径。
2. 解耦实现应先保守落地“新文件独立链路”，再逐步迁移 Web 与 CLI 调用，降低回归风险。
3. 在严格校验基础上增加“强制复用（高风险）”开关，可在不破坏安全默认值的前提下保留实验灵活性。

启发，或是下一步可以完成的工作：
- 将数据签名字段固化为统一协议，并在新训练入口、续训入口、Web 新 API 中复用同一校验实现。
- 为数据版本列表与选择页预留卡片化展示结构（主信息、关键标签、风险标识）。

补充说明：
- 风险点：强制复用可能导致训练可运行但语义不一致（如输入窗口、缩放口径、特征构造不一致）。
- 适用范围：`scripts/run` 主入口与 `web/train_web_fastapi.py` 对应训练控制流程，不含 `scripts/experiments/` 独立实验脚本。
- 限制条件：当前为决策与方案记录，尚未执行代码落地。

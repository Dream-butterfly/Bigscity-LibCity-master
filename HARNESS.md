# HARNESS.md

> GNN-TP 项目的 Harness Engineering 指南（面向 AI Coding Agent 与人机协作开发）

## 1. 目标与原则

**目标：** 让 Agent 在本仓库里稳定地产出“可运行、可验证、可维护”的改动，而不是只产出“看起来完成”的代码。

**核心原则：**
1. **基础设施优先于模型能力**：先补齐约束、反馈、工具，再追求更强模型。
2. **思考与执行分离**：先研究与计划，再编码与验证。
3. **约束必须机械化**：能用脚本/Lint/测试卡住的问题，不靠口头约定。
4. **上下文不过载**：优先精简高相关上下文，避免“信息越多越差”。

---

## 2. 四大支柱（项目落地版）

### 2.1 上下文架构（Context Architecture）

采用三层上下文：

- **Tier 1（会话常驻，最小）**
  - `HARNESS.md`（本文件）
  - `README.md`
  - 相关目录 `INFO.md`
- **Tier 2（任务按需）**
  - 具体模块代码与局部说明（如 `scripts/run/`、`web/`、`GNNTP/`）
  - 最近相关的 `ai_logs/` 记录
- **Tier 3（持久化知识库）**
  - 设计说明、历史决策、实验记录、问题复盘

**要求：**
- 单次任务只加载当前目标所需上下文。
- 大任务按子任务切分，避免长会话上下文污染。

### 2.2 Agent 专业化（Agent Specialization）

建议角色拆分（可由同一人串行扮演）：

| 角色 | 目标 | 权限建议 |
|---|---|---|
| 研究 Agent | 读代码、定位问题、收集证据 | 只读 |
| 规划 Agent | 拆任务、定义验收标准 | 只读 |
| 执行 Agent | 实现单一子任务 | 限定范围读写 |
| 审查 Agent | 检查架构/回归风险 | 只读 |
| 调试 Agent | 修复已定位缺陷 | 限定范围读写 |
| 清理 Agent | 去重、删冗余、整理文档 | 读写（受约束） |

### 2.3 持久化记忆（Persistent Memory）

进度不要只留在会话里，必须落盘：

- 变更与分析记录：`ai_logs/`
- 运行产物与结果：`outputs/`
- 数据/缓存状态：`cache/`

建议新增（按需）：
- `ai_logs/progress/current.json`：功能清单与状态（`failing/passing/blocked`）
- `ai_logs/progress/next_actions.md`：下一步动作与阻塞项

### 2.4 结构化执行（Structured Execution）

统一流程：**研究 → 计划 → 执行 → 验证 → 记录**

最小执行模板：
1. 明确目标与验收标准（可测试、可观察）。
2. 只实现一个可闭环子任务。
3. 运行对应脚本验证（训练/评估/接口/回归）。
4. 记录结果、已知风险与下一步。

---

## 3. 本仓库的硬性护栏（Guardrails）

### 3.1 代码与结构护栏

- 新增逻辑优先复用现有入口：`scripts/run/*.py`、`run_web.py`。
- 不跨层乱依赖：训练流程、Web、数据处理职责尽量清晰分离。
- 禁止“顺手重构”无关代码；只改与任务直接相关区域。

### 3.2 质量护栏

- 改动必须具备可验证结果（命令输出、指标、测试或可视化证据）。
- 不允许“未验证即宣布完成”。
- 出现失败时，先记录失败原因与复现方式，再修复。

### 3.3 上下文护栏

- 优先加载高相关文件；避免一次塞入大量历史日志。
- 会话过长时，先做摘要压缩再继续开发。

---

## 4. 典型失败模式与对策

| 失败模式 | 对策 |
|---|---|
| 一次性做太多，半途失控 | 拆成单功能闭环，逐步提交 |
| 过早宣布完成 | 强制对照验收标准逐项确认 |
| 写完即“passing”但无端到端验证 | 每项功能至少一个可复现验证步骤 |
| 新会话启动慢，反复摸索环境 | 固化启动脚本与“最小运行手册” |

---

## 5. Harness 成熟度（自评）

| Level | 状态 | 当前建议 |
|---|---|---|
| L0 无 Harness | 仅 prompt 驱动 | 避免 |
| L1 基础约束 | 文档 + 基础检查 | **当前最低目标** |
| L2 反馈回路 | CI/测试/进度追踪 | **推荐近期达到** |
| L3 专业化协作 | 多角色/分层上下文/持久化记忆 | 中期目标 |
| L4 自治循环 | 高置信无人值守 | 长期目标 |

---

## 6. 立即可执行清单（P0/P1/P2）

### P0（立刻做）
1. 将本文件作为 Agent 会话入口文档维护。
2. 固化关键任务的验收命令（训练、评估、Web 启动）。
3. 建立“失败即记录”的最小机制（`ai_logs`）。

### P1（近期做）
1. 引入结构化 `feature_list.json` 与 `progress.json`。
2. 增强自动化验证：最小回归测试 + Lint/静态检查。
3. 给常见错误补“可执行修复指令”。

### P2（中长期）
1. 增加专业化子 Agent 工作流。
2. 接入更完整可观测性（日志/指标/可视化回放）。
3. 建立定期“代码熵清理”任务。

---

## 7. 维护约定

- 该文件是**活文档**：每次出现可复发失败，都要补一条“预防规则”。
- 规则尽量简短、可执行、可验证，避免空泛口号。
- 若规则无法机械化执行，应尽快补脚本/测试/检查项。

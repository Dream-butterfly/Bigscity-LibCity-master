# STGformer-独立实现/INFO.md

## 目录职责
该目录放置 STGformer 的独立实验实现，不完全依赖主框架执行器。

## 关键内容
- `lib/`：图算法、指标、数据准备与工具函数。
- `model/`：STGformer 独立模型与训练入口。

## 输入/输出
- 输入：处理后的数据与实验参数。
- 输出：独立训练产物和评估结果（按该实现逻辑生成）。

## 调用关系
- 主要用于独立实验与对照，不是主流程默认路径。

## 修改注意事项
1. 与框架版 `STGformer/` 保持边界，避免相互污染。
2. 若迁回主框架，需补齐注册与配置对接。


# traffic_flow_prediction/INFO.md

## 目录职责
该目录汇总交通流预测任务的模型实现。

## 关键内容
- `PDFormer/`：PDFormer 模型及其执行器实现。

## 输入/输出
- 输入：交通流任务数据集批次与模型配置。
- 输出：交通流预测结果与训练损失。

## 调用关系
- 由 `models/locator` 按任务和模型名定位并实例化。

## 修改注意事项
1. 新增交通流模型时保持与数据集处理模块字段一致。
2. 注册元信息需要完整补齐。

